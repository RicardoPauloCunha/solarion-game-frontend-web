import styled from "styled-components"

export const Container = styled.span`
    display: flex;
    font-weight: 600;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    gap: 1rem;

    >div.spinner-border {
        width: 1rem;
        height: 1rem;
    }
`