import styled from 'styled-components'

export const Container = styled.strong`
    width: auto;
    color: var(--color-wine);
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 0.5rem;

    >svg {
        font-size: 1.25rem;
    }
`